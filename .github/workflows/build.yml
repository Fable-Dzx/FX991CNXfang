name: Build FX-991CN X APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle 8.7
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.7'

      - name: Decode signing keystore
        run: |
          echo "${{ secrets.KEYSTORE_B64 }}" | base64 -d > fx991cnx-release.keystore
          ls -la fx991cnx-release.keystore

      - name: Build release APK
        env:
          KEYSTORE_PATH: ${{ github.workspace }}/fx991cnx-release.keystore
          STORE_PASS: ${{ secrets.STORE_PASS }}
          KEY_PASS: ${{ secrets.KEY_PASS }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        run: gradle :app:assembleRelease --no-daemon --stacktrace --rerun-tasks

      - name: Rename APK
        run: |
          mv app/build/outputs/apk/release/app-release.apk app/build/outputs/apk/release/FX991CNX.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: FX991CNX-apk
          path: app/build/outputs/apk/release/FX991CNX.apk
          if-no-files-found: error

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: android-v1.0.${{ github.run_number }}
          name: FX-991CN X Android v1.0.${{ github.run_number }}
          body: |
            自动构建版（来自 commit ${{ github.sha }}）

            - 网页源码：https://github.com/Fable-Dzx/FX-991-CNX
            - 安装包：FX991CNX.apk（**Release 签名**，可覆盖更新）
          files: app/build/outputs/apk/release/FX991CNX.apk
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
